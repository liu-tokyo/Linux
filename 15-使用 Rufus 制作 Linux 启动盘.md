## 使用 Rufus 制作 Linux 启动盘步骤

Rufus 是在 Windows 系统下制作可引导 USB 驱动器的强大工具，它能很好地支持 Linux 发行版（如 Ubuntu、Mint 等）的 ISO 文件。

### 步骤一：准备工作

1. **下载 Rufus：** 访问 Rufus 官网下载最新的可执行文件。
2. **下载 Linux ISO：** 从您选择的 Linux 发行版（例如 Ubuntu）的官网下载 **ISO 镜像文件**。
3. **准备 USB：** 插入容量至少为 **8GB** 的 USB 驱动器。**注意：** 驱动器上的所有数据将被清除。

### 步骤二：Rufus 设置配置

运行 Rufus 后，请按照以下步骤配置选项：

1. **设备 (Device):**
   - **选择：** 确保选择了您要写入的 **USB 驱动器**。
2. **引导类型选择 (Boot selection):**
   - **选择：** 点击 **“选择”** 按钮，找到并选中您下载的 **Linux ISO 文件**。
3. **分区类型 (Partition scheme) 和 目标系统类型 (Target system):**
   - **推荐：** 建议保持默认设置，通常是 **GPT** 和 **UEFI (非 CSM)**。这是现代电脑（2010 年后）的标准引导方式。
   - **兼容性选项：** 如果目标电脑较旧，或者您不确定，可以选择 **MBR** 和 **BIOS (或 UEFI-CSM)** 以获得更广泛的兼容性。
4. **卷标 (Volume label):**
   - 您可以保留默认名称，或者输入一个易于识别的名称（例如 `Ubuntu_24_04`）。
5. **文件系统 (File system):**
   - **通常保持默认**（FAT32 或 Large FAT32），Linux ISO 写入工具会自动处理文件系统。

### 步骤三：开始写入

1. **点击“开始”：** 配置完成后，点击窗口底部的 **“开始”** 按钮。
2. **DD 模式警告（重要）：**
   - 对于许多 Linux ISO，Rufus 可能会弹出一个对话框，询问您希望以 **ISO 镜像模式 (ISO Image mode)** 还是 **DD 镜像模式 (DD Image mode)** 写入。
   - **选择：** 始终**推荐使用 DD 镜像模式** (Write in DD Image mode)。DD 模式将镜像的原始字节写入 U 盘，确保启动文件的完整性。
3. **清除警告：** Rufus 会再次警告您，U 盘上的所有数据将被销毁。点击 **“确定”** 开始写入。
4. **等待完成：** 等待进度条完成后，显示 **“就绪”**。此时，您的 Linux 可引导 USB 启动盘就制作成功了。

您现在可以使用这个 U 盘来启动电脑，并安装您选择的 Linux 系统了。